# Global Personal Memory

## Preferences
- **Git workflow**: The user prefers to handle Git commits manually to track changes themselves. Do not stage or commit files (`git add`, `git commit`) on their behalf unless explicitly instructed to do so.